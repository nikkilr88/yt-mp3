{
  "name": "yt-to-mp3",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "postinstall": "electron-builder install-app-deps",
    "start": "electron .",
    "build": "electron-packager . --out=win --overwite",
    "build:portable": "electron-builder -p never --win"
  },
  "build": {
    "files": [
      "main.js",
      "index.html",
      "client/*"
    ]
  },
  "dependencies": {
    "ffmpeg-static": "^4.0.1",
    "youtube-mp3-downloader": "^0.6.5"
  },
  "devDependencies": {
    "electron": "^8.0.3",
    "electron-packager": "^14.2.1",
    "electron-reload": "^1.5.0"
  }
}
